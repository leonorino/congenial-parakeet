<Page x:Class="working.GuestsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:working"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GuestsPage">

    <Grid Margin="10">
        <DataGrid Name="GuestsDataGrid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding first_name}"/>
                <DataGridTextColumn Header="Lastname" Binding="{Binding last_name}"/>
                <DataGridTextColumn Header="Document number" Binding="{Binding document_number}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
